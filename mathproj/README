ВСЕ ЗАПИСИ ОМУЩЕСТВЛЯЮТСЯ ЧЕРЕЗ ПРОБЕЛ НИКАКИЕ РАЗДЕЛИТЕЛИ НЕ ТРЕБУЮТСЯ. Матрицу записывать слево направо сверху вниз. Запись каждого нового задания начинается с новой строки
Данные нужно записывать в файли input.txt и копировать ответы из output.txt
1)записываем подряд все числа, что есть в строке
2)записываем коэффициенты первого числа(сначало верхний потом нижний) затем точно также для второго числа, затем степень первой дроби, степень второй дроби
3)записываем все коэффициенты уравнения(не забываем про нулевые), затем два известных корня
4)также как и в предыдущем номере записываем сначало все коэффициенты первого уравнения, затем второго
5)сначало записываем коэффициенты при а,b,c , затем сами матрицы в таком же порядке
6)записываем последовательно все значения трех матриц, затем коэффициент при третьей матрице, затем номер транспонированной матрицы
7)последовательно записываем значения матриц a,b,c 
8)записываем матрицу А, затем координаты элемента, к котрому нужно найти алгебраичекое дополнение
9)записываем матрицу из коэффициентов неизвестных, затем три свободных члена
10)просто записываем подряд все числа, что видим)
